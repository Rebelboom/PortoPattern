<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PortoPattern.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:themes="using:PortoPattern.Core.Themes"
    xmlns:converters="using:PortoPattern.Converters"
    Background="{ThemeResource Rebel.Surface.Root}"
    mc:Ignorable="d">

    <StackPanel Margin="40" Spacing="24" MaxWidth="600" HorizontalAlignment="Left" VerticalAlignment="Top">

        <TextBlock Text="Настройки" FontSize="28" FontWeight="Bold" Foreground="{ThemeResource Rebel.Text.Header}"/>

        <StackPanel Spacing="8">
            <TextBlock Text="Цветовая тема" FontSize="16" FontWeight="SemiBold" Foreground="{ThemeResource Rebel.Text.Primary}"/>
            <TextBlock Text="Выберите оформление интерфейса приложения." FontSize="13" Foreground="{ThemeResource Rebel.Text.Secondary}"/>

            <ComboBox Width="300"
                      ItemsSource="{x:Bind ViewModel.AvailableThemes, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.CurrentTheme, Mode=TwoWay}"
                      Margin="0,8,0,0">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="themes:AppTheme">
                        <TextBlock Text="{x:Bind Converter={StaticResource AppThemeConverter}, Mode=OneWay}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

    </StackPanel>

</Page>