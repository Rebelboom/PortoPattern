<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PortoPattern.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:history="using:PortoPattern.Core.History"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" 
                    Style="{StaticResource NavigationBackButtonNormalStyle}"
                    Command="{x:Bind ViewModel.GoBackCommand}"
                    Margin="0,0,16,0"/>

            <TextBlock Grid.Column="1" 
                       Text="История сканирований" 
                       Style="{StaticResource TitleTextBlockStyle}" 
                       VerticalAlignment="Center"/>

            <Button Grid.Column="2" 
                    Content="Очистить историю" 
                    Command="{x:Bind ViewModel.ClearHistoryCommand}"
                    IsEnabled="{x:Bind ViewModel.IsEmpty, Converter={StaticResource BoolNegationConverter}, Mode=OneWay}"/>
        </Grid>

        <TextBlock Grid.Row="1" 
                   Text="История сканирований пуста."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{x:Bind ViewModel.IsEmpty, Converter={StaticResource BooleanVisibility}, Mode=OneWay}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

        <ListView Grid.Row="1" 
                  ItemsSource="{x:Bind ViewModel.HistoryItems, Mode=OneWay}"
                  Visibility="{x:Bind ViewModel.IsEmpty, Converter={StaticResource BooleanVisibilityInverse}, Mode=OneWay}"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="history:ScanHistoryItem">
                    <Grid Padding="16" Margin="0,4,0,4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{x:Bind TargetPath}" FontWeight="SemiBold" FontSize="16" TextTrimming="CharacterEllipsis"/>
                            <!-- Исправлено здесь: используем FormattedScanTime -->
                            <TextBlock Text="{x:Bind FormattedScanTime}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Spacing="24">
                            <StackPanel>
                                <TextBlock Text="Файлы" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind TotalFiles}" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Категории" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{x:Bind CategoriesCount}" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>